class WeatherInfo{
  String desc;
  String name;
  int temp;
  double tempFeel;
  double tempMin;
  double tempMax;
  WeatherInfo(this.temp, this.name, this.desc, this.tempMax,this.tempFeel, this.tempMin);
}